[project]
name = "replace-me"
revision = "1.0"
require_reason = "agents"

[providers]
local = "keyring://"
ci = "env://"

[profiles.default]
API_TOKEN = { description = "Token used by the example application", providers = [
  "local",
] }

[profiles.ci]
API_TOKEN = { providers = ["ci"] }
